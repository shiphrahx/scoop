# @vitejs/plugin-react 6 declares a peer range that excludes the vite 7 that
# vitest 3 pulls in. The combination works — vitest resolves it itself — but npm
# refuses to build the tree without this. Set here rather than passed on the
# command line so a local install and CI's `npm ci` resolve the same way.
legacy-peer-deps=true
